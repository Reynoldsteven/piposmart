# Piposmart — Frontend

Aplikasi Flutter untuk Piposmart.

Dokumentasi lengkap (instalasi, struktur, API, database): lihat **[README.md](../README.md)** di root repository.

```bash
flutter pub get
flutter run
```

Atur base URL di `lib/config/api_config.dart` sebelum menjalankan.
